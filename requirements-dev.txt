# Test-only dependencies (install with: uv pip install -r requirements-dev.txt)
# Production Kittyflap installs requirements.txt only — do not merge these there.
pytest>=8.0
pytest-timeout>=2.3
pytest-cov>=5.0
